{
  "identifier": "default",
  "description": "Default capability for the Realm Agent Studio main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "core:window:allow-set-title",
      "allow": [{ "label": "main" }]
    }
  ]
}
